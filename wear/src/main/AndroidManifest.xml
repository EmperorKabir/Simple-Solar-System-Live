<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- REQUIRED, direct child of <manifest>: marks this as a Wear OS app. -->
    <uses-feature android:name="android.hardware.type.watch" />
    <!-- Round is the target face, but DO NOT require it (required="true" excludes
         every round watch on Play -> 0 supported devices). -->
    <uses-feature android:name="android.hardware.screen.round" android:required="false" />

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:icon="@drawable/ic_solar"
        android:label="@string/app_name"
        android:allowBackup="false"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">

        <!-- Usable without a paired phone; still ships under the same listing. -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <service
            android:name=".SolarWatchFaceService"
            android:exported="true"
            android:label="@string/watch_face_name"
            android:directBootAware="false"
            android:permission="android.permission.BIND_WALLPAPER">
            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService" />
                <category android:name="com.google.android.wearable.watchface.category.WATCH_FACE" />
            </intent-filter>
            <meta-data
                android:name="com.google.android.wearable.watchface.preview"
                android:resource="@drawable/watch_preview" />
            <meta-data
                android:name="android.service.wallpaper"
                android:resource="@xml/watch_face" />
        </service>
    </application>
</manifest>
